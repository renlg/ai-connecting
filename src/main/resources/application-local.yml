# 本地开发配置 - 使用方式: --spring.profiles.active=local
# 此文件包含开发环境的默认值，请勿在生产环境使用

spring:
  data:
    redis:
      host: localhost
      port: 6379
      username:
      password:

app:
  rate-limit:
    enabled: false
  jwt:
    secret: dev-local-secret-key-must-be-at-least-256-bits-long-for-hs256
  admin:
    default-password: admin123
    reset-password: 88888888
